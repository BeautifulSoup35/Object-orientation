

컴포지션: 몸과 심장 // 전체가 완전히 소유를 한다
이그리게이션 : 교수와 강의 //전체가 부분을 소유한다

자동차와 엔진은 컴포지션관계 이 엔진이라는게 독립적으로 존재 불가능 그래서 엔진이 필드에 포함되어있다


main에서 자동차를 인스턴스화하면 자동으로 엔진도 생성된다 

이그리게이션
학과에 소속된 강사
교수 만들고 

가상함수 : 파생 클래스에서 재정의 virtual 키워드 사용 부모에서 선언되있으면 자식이 그냥 사용하거나 재정의 해서 사용
          실행시간에 
동적바이딩 : anial* ptr1 = &dog;
            ptr1->speak()//멍멍
            anial* ptr2 = &cat;
            ptr2->speak()//냐옹


가상소멸 과동적바이딩 
가상소멸자 자식를 해제 개념정리 

정적맴버 변수
정적멤버 함수

정적맴버 변수의저의 -> 클래스 밖에서

함수 템플
